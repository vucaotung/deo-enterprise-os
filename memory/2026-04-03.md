# 2026-04-03

## Kiến trúc deo-enterprise-os V6 — Huly Collab

### Context
Sếp gửi 5 files:
- `DEO_OS_FINAL_ARCHITECTURE.md` (bản kiến trúc cũ)
- `002_deo_schema.sql`
- `004_many_to_many.sql`
- `005_orchestration_upgrade.sql`
- `003_deo_v4_update` (không đọc chi tiết)

Yêu cầu: đọc bản kiến trúc FINAL, suy nghĩ như senior architect, update bản mới theo phương án collab với Huly.

### Công việc đã làm
1. Đọc toàn bộ `DEO_OS_FINAL_ARCHITECTURE.md` (bản cũ)
2. Đọc 3 file schema: `002`, `004`, `005`
3. Phân tích điểm mạnh/yếu của bản cũ khi nhìn theo góc Huly
4. Viết bản kiến trúc mới: **`C:\deo-enterprise-os\docs\DEO_OS_ARCHITECTURE_V6_HULY_COLLAB.md`**

### Kết luận chính V6

**Bản cũ đúng hướng** ở:
- chat-first
- context engine
- clarification thay vì đoán
- 2-layer states
- orchestration cho human + AI

**Nhưng thiếu** khi nhìn theo Huly:
- chưa tách rõ platform core vs domain modules
- chưa coi document/file/search/export là subsystem hạng nhất
- chưa có transaction core / transactor pattern
- chưa tách rõ sync path vs async side effects
- data layer còn lẫn (Postgres + Redis + Drive + local files)
- chưa có integration layer rõ nghĩa

### Kiến trúc V6 mới — 5 layers

```
1. Interaction Layer (Telegram, Zalo, webapp chat, dashboard, email)
2. Platform Core (auth, users, companies, permissions, audit, events, conversations, context)
3. Domain Modules (CRM, tasks, finance, legal, files)
4. Process/AI Layer (orchestration, agents, clarifications, notebooks)
5. Platform Services (storage, document intelligence, export, search, notify, integrations)
```

### 10 thứ học từ Huly (áp ngay)
1. Platform core rõ ràng
2. Transaction core
3. Storage abstraction
4. Document intelligence là service riêng
5. Async side effects
6. Search như một layer riêng
7. Integrations là layer riêng
8. Real-time là infra
9. Typed client / contract thinking
10. Docs kiến trúc sống

### 5 thứ KHÔNG copy từ Huly
1. 30+ microservices
2. CockroachDB
3. Redpanda/Kafka quá sớm
4. Build system quá nặng
5. Full CRDT collab ngay

### Package structure mới (target)
```
packages/
├── platform-core/
├── domain-{crm,tasks,finance,legal,files}/
├── orchestration/
├── service-{storage,docint,export,search,notify}/
├── integration-{google,telegram,zalo,n8n}/
└── vietnam/
```

### Schema updates đề xuất
- `files`: thêm storage_provider, storage_key, checksum, extracted_text, preview_status, index_status
- `audit_events`: formalize event namespace (task.created, contract.review.completed, etc.)
- `messages`: thêm external_message_id, reply_to_message_id, delivery_status
- Tách orchestration jobs vs platform service jobs (có thể dùng system_jobs table sau)

### Sync vs Async pattern
**Sync path chỉ làm:**
- authenticate
- validate business rule
- write transaction
- emit event

**Async làm:**
- indexing, preview, export, notification, AI follow-up, analytics

### Roadmap mới
- Phase A: Ổn repo + deploy nền
- Phase B: Formalize platform core
- Phase C: Orchestration V2
- Phase D: Huly-inspired platform services
- Phase E: UI control center

### Câu chốt V6
> deo-os V6 = ClawTask primitives + Viet-ERP packaging + Huly platform architecture  
> Deploy: modular monolith + worker + storage abstraction + async side effects  
> Mục tiêu: vừa deploy nhanh 1 VPS vừa có bộ xương đủ chuẩn để lớn thành business platform thật

### Files cần tạo tiếp theo
1. `docs/ARCHITECTURE.md` (bản clean cho repo)
2. `docs/DECISIONS.md` (ADRs mới theo V6)
3. `docs/PLATFORM_SERVICES.md` (storage / docint / export / search / notify)
4. `docs/ORCHESTRATION.md` (agents / clarifications / notebooks / protocol)
5. `infrastructure/postgres/006_platform_services_upgrade.sql` (nếu chốt schema bổ sung)

### Trạng thái cuối
Sếp chưa chỉ định việc tiếp theo. Đang chờ feedback hoặc yêu cầu mới.
