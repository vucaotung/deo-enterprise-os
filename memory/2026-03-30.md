# Memory - 2026-03-30

- Đã nghiên cứu cơ chế giao việc giữa các agent trong OpenClaw và chốt kiến trúc đúng là **DB job queue / orchestrator**, không dựa vào cross-message native giữa bot-agent.
- Đã thiết kế và chạy thành công migration `005_agent_job_queue.sql`, tạo 3 bảng mới trong schema `deo`:
  - `deo.agent_jobs`
  - `deo.agent_job_runs`
  - `deo.agent_job_messages`
- Đã tạo tài liệu thiết kế tại `C:\Users\Admin\.openclaw\workspace\AGENT_JOB_QUEUE_DESIGN.md`.
- Đã implement API route `apps/api/src/routes/agent-jobs.ts` với các endpoint create/list/detail/update/message/retry cho job queue và mount route này vào `apps/api/src/index.ts`.
- Đã implement worker mẫu cho `agent-phap-che` tại `C:\Users\Admin\deo-enterprise-os\apps\api\legal-worker.js`.
- Đã test thành công flow end-to-end: `agent-admin` tạo job `legal_review` → `agent-phap-che` worker poll job → xử lý → ghi result vào DB.
- Test job thành công với ID: `3e1ca258-34e6-45d3-bf9e-33f6123f1ac4`.
- Đã gặp lỗi schema thật do trigger `deo.update_timestamp()` yêu cầu cột `updated_at` nhưng bảng `deo.agent_jobs` chưa có; đã fix bằng cách thêm cột `updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()` vào `deo.agent_jobs`.
- Bài học vận hành: khi tái dùng trigger `update_timestamp()` cho bảng mới, phải bảo đảm bảng có cột `updated_at`, nếu không worker/update query sẽ fail với lỗi `record "new" has no field "updated_at"`.
- Heartbeat ngày 2026-03-30 đã chạy Weekly Expense Digest theo `HEARTBEAT.md`; đã tổng hợp chi tiêu tuần trước và phát hiện tab `Transactions` trong Google Sheet hiện có dữ liệu **không đồng nhất format**, khiến phải parse thủ công để ra digest.
- Tổng chi digest tuần trước (24–30/03/2026) đã gửi: `3.328.995 VND`; cần nhớ rằng pipeline expense digest hiện chưa đủ ổn định nếu dữ liệu sheet tiếp tục lẫn nhiều format khác nhau.
