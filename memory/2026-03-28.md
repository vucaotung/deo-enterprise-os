# 2026-03-28

- Làm việc với sếp về việc migrate n8n từ SQLite sang Postgres (`deo_os` trên `127.0.0.1:5433`) và xác nhận n8n đã lên lại ở `http://localhost:5678`.
- Đã tạo/nhập các workflow trong n8n gồm `DEO_TEST_HELLO`, `DEO_EXPENSE_LOGGING_SKELETON`, `DEO_EXPENSE_LOGGING_REAL`, và `DEO_EXPENSE_LOGGING_V2`.
- Phát hiện workflow `DEO_EXPENSE_LOGGING_REAL` không activate được vì instance n8n hiện không nhận node `n8n-nodes-base.executeCommand`; đã tắt workflow này và restart n8n để làm sạch log.
- Dựng `DEO_EXPENSE_LOGGING_V2` theo hướng sạch hơn: webhook + normalize expense + build response; chưa nối Google Sheets node thật vì credential Google trong n8n chưa tạo xong.
- Xác nhận trong n8n hiện chưa có Google credential nào (`n8n export:credentials --all --decrypted` trả `No credentials found`).
- Hướng dẫn sếp cấu hình Google OAuth cho n8n local: dùng OAuth Client kiểu `Web application`, origin `http://localhost:5678`, redirect URI `http://localhost:5678/rest/oauth2-credential/callback`, app ở chế độ Testing.
- Chốt account sếp sẽ dùng để connect Google trong n8n là `vucaotung@gmail.com`; cần thêm chính account này vào OAuth consent screen > Test users.
