# 2026-04-07

- Personal finance direction was formally split into a separate sub-domain `pf` (Personal Finance) under repo `vucaotung/deo-enterprise-os` while still tracking changes in the main repo.
- GitHub tracking was created in `vucaotung/deo-enterprise-os` as issue `#1 [pf] Start Phase 1 - Personal Finance OS workbook design`.
- Workbook name for the new finance system was finalized as **`Personal Finance OS`**. Decision: discard the old finance sheet as the design base and build a new workbook from scratch.
- A local workbook scaffold `Personal Finance OS.xlsx` was generated in workspace with tabs:
  - `Transactions`
  - `Accounts`
  - `Categories`
  - `Debts`
  - `Investments`
  - `Rules`
  - `Settings`
  - `Attachments`
  - `Import Logs`
  - `Dashboard Overview`
  - `Dashboard Cashflow`
  - `Dashboard Accounts`
  - `Dashboard Debts`
  - `Dashboard Investments`
- Gog CLI authentication for `vucaotung@gmail.com` was eventually proven working, but `gog status -j` without explicit account was misleading. Important operational note: use `./gog.cmd status -a vucaotung@gmail.com -j` to verify the actual logged-in account.
- The workbook was uploaded/converted to Google Sheets and standardized as the live file:
  - title: `Personal Finance OS`
  - spreadsheet id: `1tsSsM3n_oHOeoBMTjRZ4VlE7S2mwV4kK9OcFLccl3RA`
  - final live link: `https://docs.google.com/spreadsheets/d/1tsSsM3n_oHOeoBMTjRZ4VlE7S2mwV4kK9OcFLccl3RA/edit?usp=drivesdk`
- A temporary empty Google Sheet created for auth testing was deleted/trashed:
  - id: `1ZmT0iLRZgblq5_YQSAIm4MiMeHI3thmySkx37Iiu1YQ`
- A1 was completed on the live Google Sheet by updating spreadsheet timezone to **`Asia/Bangkok`** through the Sheets API.
- A2 was completed by mapping real data from the legacy sheet `So chi tieu - Deo` (`1iMkXCCBECU4cvtnavGFjfV5xoe2CID_0Y5CROb0RD9E`) into the new `Personal Finance OS` live sheet:
  - real accounts imported (MB BANK, Vietcombank, TPBank accounts, VPBank, cash)
  - categories aligned to the old sheet’s real usage (Ăn uống, Cà phê, Đổ xăng, Đi lại, Mua sắm, Hóa đơn, Trả nợ, Lương, Thưởng, Khác)
  - one real debt imported: `Khoản vay TPBank kỳ 25-03-2026`
  - old receivable `Phải thu từ Luân lùn` was intentionally **not** forced into the `Debts` schema; decision is to model this as a future `Receivables` module in v2.
- A3 was completed by inserting 5 representative test transactions into the live sheet to validate the new model:
  - expense / eating
  - expense / fuel
  - income / salary
  - debt payment / TPBank
  - transfer between accounts
- The A3 test proved the **data path and schema path** are viable, but the formula/dashboard layer failed after Excel-to-Google conversion.
- During A4/A4.1 investigation, the real root cause was identified: the live Google Sheet uses locale **`vi_VN`**, so formulas must use semicolon separators (`;`) instead of comma separators (`,`). Prior attempts failed mainly because formulas were pushed in English/US syntax while the file locale remained Vietnamese.
- Additional lesson from A4.1: one repair attempt accidentally wrote helper formulas into the wrong ranges, temporarily overwriting `Transactions.type` and `Transactions.destination_account_id`. This was diagnosed by reading back live formulas from the sheet via Sheets API.
- Current next-step state for finance workbook: proceed with **A4.2**, which means rewriting the core formula layer in true Google Sheets `vi_VN` syntax for:
  - `Transactions` helper columns (`month_key`, `year`, `week_key`, lookup names)
  - `Accounts.current_balance`
  - `Dashboard Overview`
- Heartbeat weekly expense digest was triggered, but current readable data in the legacy expense sheet showed no rows within the previous-week window `2026-03-30` to `2026-04-05`, so the digest returned totals of zero for that week.
- A14.8b was completed on the live `Personal Finance OS` Google Sheet by repairing the `Quick Entry -> Transactions` append path. Root cause: the append script had started requiring an explicit target row (`tx_rownum`) for formula-safe writes, but one call site still used the old signature, causing cron/manual append failures. The fix changed the writer to compute explicit target rows and write directly to `Transactions!A:Y` instead of relying on ambiguous append placement.
- Important finance truthfulness lesson: a Quick Entry row marked `posted` is not enough to claim the transaction is in the main ledger. Live verification against `Transactions` is required before claiming success.
- Two early validation transactions were fully repaired and verified live in `Transactions`: `TXN-20260406-QE-031` (income 2,000,000,000 VND, category `CAT-INC-003 / Nhận tiền ứng`, destination `MB BANK`, scope `business`) and `TXN-20260406-QE-032` (expense 499,000,000 VND from `MB BANK`, project advance, scope `business`). Dashboard Overview then correctly showed income 2,000,000,000; expense 499,000,000; net cashflow 1,501,000,000.
- OCR/bill intake validation improved: Aurora successfully created and posted real ledger transactions `TXN-20260406-QE-033` (450,000,000 VND) and `TXN-20260406-QE-034` (82,842,820 VND) for steel/project spending to `Transactions`, with dashboard totals updating live. This proved Aurora can now push true ledger entries for expense bills, not just Quick Entry stubs.
- A new business expense category was added to the live sheet: `CAT-EXP-007 / Chi dự án` with default scope `business`, intended for project/material/site/construction spending.
- A14.8c was completed by adding live categorization rules to `Rules` for project-related text patterns: `THEP`, `VAT TU`, `CONG TRINH`, and `DU AN` now map to `CAT-EXP-007 / Chi dự án` with scope `business`.
- Historical project-expense rows `TXN-20260406-QE-032`, `TXN-20260406-QE-033`, and `TXN-20260406-QE-034` were recategorized from `Khác` to `Chi dự án` in both `Transactions` and corresponding `Quick Entry` rows, cleaning up reporting semantics without changing cash totals.
- Aurora successfully posted another real transaction `TXN-20260406-QE-035` (expense 5,000,000 VND from `MB BANK`, description `Trả lương cho Tâm`) into `Transactions`; dashboard totals updated live. However, classification remained weak: it still landed in fallback category `CAT-EXP-999 / Khác`, showing the ledger path is working but semantic categorization still needs a later A14.8d-style refinement for salary/advance/material buckets.
- OCR reliability lesson for Aurora: when image OCR cannot reliably extract amount/date/account, the correct fallback is to say OCR failed clearly and ask the user for one compact text line to complete bookkeeping immediately.
