# 2026-04-08

- Calendar near-time sweep automation: `gog.exe calendar events --from now --to "+4h"` failed because local gog time parser on this host does not accept relative `+4h` for `--to`. Working fallback: pass an absolute end timestamp instead (for example `2026-04-08 12:09`).
