# 2026-04-05

- Dẹo và sếp tiếp tục chốt hướng phát triển `deo-enterprise-os` theo roadmap phase-based; Phase 1 hiện tập trung canonicalize web app foundation trước khi mở rộng domain.
- Đã hoàn tất các bước nền của Phase 1 web canonicalization cho auth/app shell:
  - align `useAuth.ts` với `api/client.ts`
  - align `Login.tsx` với auth flow mới
  - thêm `PublicRoute`/`PrivateRoute` rõ ràng trong `apps/web/src/App.tsx`
  - chuẩn hóa hiển thị user ở `Layout.tsx` và `Sidebar.tsx`
  - dọn legacy API usage rõ nhất ở `Dashboard.tsx`
- Sếp chọn tạm hoãn cleanup `Finance.tsx`; không tiếp tục step 1.5 ngay.
- Đã audit các hub page còn lại trong web app và chốt rằng phần debt hiện tập trung ở các page dùng mock/demo, đặc biệt `Finance.tsx`, `Agents.tsx`, `Notebooks.tsx`; `Finance.tsx` được xem là mục tiêu cleanup ưu tiên số 1 nếu quay lại Phase 1 cleanup page-level.
- Đã tạo và push các tài liệu kiến trúc/web planning quan trọng trong repo:
  - `docs/WEB_APP_REFACTOR_MAPPING.md`
  - `docs/WEB_APP_CANONICALIZATION_PLAN.md`
  - `docs/PHASE1_STEP14_AUDIT.md`
  - `docs/PROJECT_MANAGEMENT_DOMAIN_V1.md`
- Đã chốt domain mới quan trọng: `Project Management` phải gắn chặt với `Task Management` theo nguyên tắc:
  - Project = khung quản trị
  - Task = đơn vị thực thi
- Trong `PROJECT_MANAGEMENT_DOMAIN_V1.md`, Dẹo đã xác định project là trục kết nối giữa CRM, Tasks, Clarifications, Knowledge, Finance và Agents; đây là định hướng lâu dài quan trọng cho kiến trúc sản phẩm.
- Route/domain direction đã được chốt cho project management v1:
  - `/projects`
  - `/projects/:id`
  - `/projects/:id/tasks`
  - `/projects/:id/clarifications`
  - `/projects/:id/notebooks`
  - `/projects/:id/finance` (về sau)
- Nếu tiếp tục nhánh này, bước hợp lý tiếp theo là viết `PROJECT_MANAGEMENT_WEB_STRUCTURE.md` để chuyển từ domain doc sang cấu trúc web app cụ thể.
- Trong ngày 2026-04-05, sếp đã chốt tạm dừng nhánh web/project-task đang làm sau khi hoàn tất bước đang dang dở; trọng tâm chuyển sang nhánh kiến trúc `agent domain` + `n8n`.
- Đã hoàn tất và push cả một cụm docs kiến trúc cho nhánh chat/orchestration để làm stop-line rõ ràng trước khi rẽ hướng, gồm:
  - `docs/CHAT_V1_IMPLEMENTATION_PLAN.md`
  - `docs/AGENT_DOMAIN_V1.md`
  - `docs/N8N_ROLE_IN_ENTERPRISE_OS.md`
  - `docs/AGENT_TO_N8N_EXECUTION_PATTERN.md`
  - `docs/N8N_WORKFLOW_REGISTRY_V1.md`
  - `docs/AGENT_ADMIN_MODEL_V1.md`
  - `docs/AGENT_RUN_AND_CALLBACK_TRACE_MODEL.md`
- Nhánh kiến trúc hiện đã chốt 2 cụm lớn tương đối hoàn chỉnh:
  1. Chat / Telegram as Work OS / thread orchestration
  2. Agent / n8n / workflow registry / control plane / execution trace
- Các nguyên tắc kiến trúc quan trọng đã được khóa:
  - Telegram group là human-native frontstage cho Work OS, không ép user bỏ Telegram/Zalo để vào web chat riêng.
  - Agent là actor thực thi có role/capabilities/runtime state/bindings/invocation trace; không phải chỉ là chatbot avatar.
  - n8n là workflow engine + integration fabric đứng dưới orchestration layer; `truth ở OS, flow ở n8n`.
  - Workflow phải đi qua registry và trace model rõ ràng, không gọi ad-hoc.
  - Agent Admin là control plane, tách biệt với Agent Domain là bản thể.
- Repo `deo-enterprise-os` trên GitHub đã được cập nhật liên tiếp với các commit mốc của nhánh agent+n8n:
  - `6227550` — chat v1 implementation plan
  - `fd1c77a` — agent domain v1
  - `4027fe4` — n8n role in enterprise os
  - `5ccd4df` — agent to n8n execution pattern
  - `125a841` — n8n workflow registry v1
  - `dfd8733` — agent admin + execution trace model
- Hướng hợp lý tiếp theo, nếu tiếp tục sau này, là chuyển từ cụm docs architecture sang implementation planning thực dụng cho agent/n8n, ví dụ `AGENT_V1_IMPLEMENTATION_PLAN.md`, `N8N_INTEGRATION_IMPLEMENTATION_PLAN.md` hoặc một doc tích hợp kiểu `ORCHESTRATION_STACK_V1.md`.
- Trong cùng ngày 2026-04-05, trọng tâm repo `deo-enterprise-os` đã chuyển mạnh sang nhánh **back office / Google Workspace generation** thay vì tiếp tục đào sâu `Project Management + Task Management` ở ngay vòng này.
- Nhánh back office đã chốt các nguyên tắc kiến trúc quan trọng:
  - hướng **workflow-first, Google-native**
  - không dùng `LibreOffice` / `python-docx` làm lõi
  - n8n nằm ở execution/integration layer
  - agent quyết định, workflow registry là cổng gọi chuẩn, Work OS giữ metadata/source-of-truth
  - tách 2 lane `template_based` và `draft_from_context`
  - luồng context-based phải có review checkpoint / human-in-the-loop trước khi materialize file thật
- Đã chốt chuẩn naming và cây thư mục Drive cho back office, với root `Deo Workspace/` và các nhóm như `00_dieu hanh`, `01_templates`, `02_ho so theo mau`, `03_ho so theo yeu cau`, `04_du an`, `05_khach hang`, `06_tai chinh`, `07_bao cao`, `08_luu tam`, `09_luu tru`.
- Build order thực dụng cho back office đã được chốt là: làm `registry + metadata + callback` trước, rồi mới workflow tạo file thật; ưu tiên `drive.resolve-folder.v1` và `docs.from-template.v1`.
- Callback route back office đã được chốt là **không bị chặn bởi auth mount toàn cục**; `/api/backoffice` mount không kèm `authMiddleware`, auth xử lý ở từng route.
- `POST /api/backoffice/docs/from-template` đã được chốt là sẽ **tự resolve folder nếu caller không truyền `target_folder`**.
- Trong repo đã được viết và push thêm cả cụm docs/scaffold thực chiến cho back office, gồm các artifact tiêu biểu:
  - `infrastructure/n8n/docs.from-template.v1.workflow.skeleton.json`
  - `docs/N8N_NODE_BUILD_CHECKLIST_docs.materialize.v1.md`
  - `docs/N8N_IMPORT_AND_CONFIG_GUIDE_docs.from-template.v1.md`
  - `docs/BACKOFFICE_CURL_TESTS_V1.md`
  - `docs/BACKOFFICE_E2E_TEST_PLAYBOOK_V1.md`
  - `docs/N8N_LOCAL_IMPORT_AND_GOOGLE_CREDENTIAL_QUICKSTART.md`
- Các commit mốc mới của nhánh back office trong ngày gồm:
  - `c9e624e` — docs: add n8n workflow skeleton and materialize checklist
  - `f943932` — docs: add n8n import guide and curl tests
  - `746caf8` — docs: add backoffice e2e test playbook
  - `1e18920` — docs: add local n8n import quickstart
- Local machine của sếp đã xác nhận có **n8n cài global npm** và chạy được bằng CLI; version đang thấy là **`2.13.4`**.
- Local n8n đang chạy thật trên **`http://127.0.0.1:5678`**, port `5678` có listen và web UI trả `200`.
- Local n8n hiện là instance đã setup xong user management, có DB thật ở `C:\Users\Admin\.n8n\database.sqlite`, có encryption key ở `C:\Users\Admin\.n8n\config`, và settings cho thấy:
  - `authenticationMethod = email`
  - `showSetupOnFirstLoad = false`
  - `smtpSetup = false`
- Account login n8n local hiện thấy trong DB là **`vucaotung@gmail.com`** (tên `Cao Tung Vu`); password trong DB chỉ lưu dạng **bcrypt hash**, không đọc ngược ra plaintext được.
- Do `smtpSetup = false`, local n8n hiện **không có forgot-password/reset-by-email usable**; nếu quên mật khẩu thì hướng practical là reset local, nhưng tối cùng ngày sếp đã xác nhận tự đăng nhập được `localhost:5678` nên chưa cần reset.
- Khi rà DB local n8n trong ngày, Dẹo chỉ thấy workflow cũ `DEO_TEST_HELLO` và **chưa thấy credential records**, nên local instance nhiều khả năng **chưa có Google credentials sẵn** cho Drive/Docs; nếu đi tiếp phải tạo credential Google trong n8n.
- Cuối phiên, sếp đã bắt đầu thao tác trong UI n8n local (tạo/chọn credential cho node `Apply Placeholder Replacements` và save), nhưng Dẹo vẫn chưa xác nhận được workflow `docs.from-template.v1` đã persist vào DB; khi quay lại cần tiếp tục từ bước xác minh workflow đã xuất hiện trong workflow list/local DB rồi mới bắn test execution thật.

## 2026-04-05 21:24 ICT - n8n docs.from-template test findings
- User sent another legacy Word sample mau_bao_gia_so_1---a3824d34-4d7e-4f4b-b35f-1afac2f4afa2; uploaded/converted successfully to Google Doc with ID 1mQSrb4FAxqFkNTJyJWfuFt1CLX7knL--HmWTIDnL_zo and mapped as ao_gia_v1.
- Ran a live test request against local n8n webhook http://127.0.0.1:5678/webhook/backoffice/docs/from-template for 	emplate_key = hop_dong_dich_vu_v1; webhook returned HTTP 200 but the workflow did not complete.
- Initial diagnosis about Copy Template Document from screenshot was wrong; user corrected that the failing node is Guard Check.
- Current practical conclusion: the workflow is failing at input validation (Guard Check) before any Google Drive copy step; next step is to patch or relax Guard Check to reveal actual inbound body shape / missing fields, then re-test.
- Suggested debug patch for Guard Check: improve error messages for missing payload, payload.template_key, payload.title, callback, callback.url; temporary bypass option 
eturn .all(); was proposed only as a fast debug shortcut.
- Later in the same debugging chain, confirmed that the n8n admin API on local instance requires header `X-N8N-API-KEY` (not `Authorization: Bearer ...`) and that workflow `docs.from-template.v1` is readable via `http://127.0.0.1:5678/api/v1/workflows/FFbPW0MILSfS6qi3`.
- By reading the live workflow JSON from the n8n API, Dẹo confirmed the active workflow still had two real defects:
  - `Apply Placeholder Replacements` in the live workflow was still configured with a hardcoded Google Docs `batchUpdate` URL pointing to doc `1aU7Woes8oopS9wKBHbGL3wDTzfu0AH28VEKDyHJv6aQ` and a hardcoded test body containing an empty placeholder text, instead of using dynamic `{{$json.id}}` / `{{$json.requests}}`.
  - `Build Replace Requests` in the live workflow was still using a shallow `const data = item.json.data ?? {};` pattern, so if `data` was lost after the copy node or nested elsewhere, it produced empty `requests`.
- The API payload also showed active/live-vs-version mismatch clues: current workflow graph used node name `Copy template document`, while `activeVersion` still showed an older `Copy file` node and earlier code variants. Practical lesson: some UI edits had not actually landed in the active workflow version being executed.
- Dẹo attempted to patch the workflow directly through the n8n API using `PUT /api/v1/workflows/FFbPW0MILSfS6qi3`, but local n8n returned HTTP `400` with no response body. Result: read access via API works, but write/update schema still needs exact payload shape or another patch path.
- Practical next-step patch already derived from the live JSON: replace `Build Replace Requests` with a fallback version that rehydrates `data` from upstream nodes (`Resolve Template ID` / `Normalize Input`) and update `Apply Placeholder Replacements` to dynamic URL/body (`https://docs.googleapis.com/v1/documents/{id}:batchUpdate` with body `{{ { requests: $json.requests } }}`).
