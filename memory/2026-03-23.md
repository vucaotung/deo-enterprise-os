## 2026-03-23 — 9router + gog skill

- Sếp cài và kiểm thử `9router` trên Windows.
- Xác nhận `9router` cài global qua npm, package ở `C:\Users\Admin\AppData\Roaming\npm\node_modules\9router`, dữ liệu chính ở `C:\Users\Admin\AppData\Roaming\9router\db.json`, có thư mục phụ `C:\Users\Admin\.9router`.
- `db.json` của 9router chứa token/provider secrets thật; cần coi là file cực nhạy cảm.
- 9router có autostart qua `C:\Users\Admin\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\9router.vbs`.
- Dashboard 9router ban đầu hiện password mặc định `123456`; sau đó sếp đã tự đổi password.
- Runtime 9router trên Windows có lỗi log: `[usageDb] Failed to get user data directory: File URL path must be absolute`, nhưng server vẫn lên và phục vụ request bình thường.
- Cấu hình production Dẹo khuyên cho 9router:
  - giữ combo `Combo1 = cx/gpt-5.4 -> qw/qwen3-coder-plus -> if/qwen3-coder-plus`
  - giữ Codex x3 active khi dùng bình thường
  - bật Round Robin
  - ưu tiên `Sticky Limit = 1` để chia tải đều và né limit tốt hơn
  - giữ Qwen + iFlow active làm fallback
  - giữ Observability ON, Outbound Proxy OFF
- Đã kiểm thử thật fallback 3 tầng của `Combo1`:
  - khi Codex còn sống, `Combo1` trả `gpt-5.4`
  - khi Codex tắt sạch, `Combo1` rơi xuống Qwen và trả `qwen3-coder-plus`
  - khi Codex + Qwen đều tắt, `Combo1` rơi xuống iFlow; response vẫn mang model name `qwen3-coder-plus` do iFlow đang serve model đó.
- Đã xác nhận acc Codex 1 từng bị 429/unavailable trong dashboard; khi acc 1 unavailable và acc 2 off, acc 3 vẫn gánh request Codex ổn.
- Sếp nhờ gỡ sạch `ProxyPal` và `cli-proxy-api` khỏi máy Windows; đã tắt process, xoá `C:\Antigravity\ProxyPal` và `C:\Users\Admin\AppData\Roaming\proxypal`, rồi hậu kiểm thấy không còn shortcut/proxy system config treo.
- Đã tạo local skill mới `gog` tại `C:\Users\Admin\.openclaw\workspace\skills\gog\SKILL.md` để chuẩn hóa workflow dùng `gog.cmd/gog.exe/gogcli` cho Gmail, Calendar, Drive theo đúng policy:
  - Gmail + Calendar dùng `vucaotung@gmail.com`
  - Drive ghi dùng `zman8585@gmail.com`
  - Drive chính `vucaotung@gmail.com` là chỉ đọc mặc định.
