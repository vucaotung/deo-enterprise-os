{
  "name": "deo-enterprise-os",
  "private": true,
  "version": "3.2.0-dev",
  "description": "Dẹo Enterprise OS — Phase 0 v2 monorepo (rebuild per ADR-13)",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
