# apps/web-legacy

⚠️ **FROZEN — DO NOT MODIFY**

Codebase v1.2.0-dev cũ. Đóng băng theo ADR-13.

- **Tag git:** `v1.2.0-dev-frozen` (commit `b67d6b2`)
- **Production v0.2.3** vẫn dùng codebase này. Cutover sang `apps/web` mới khi Phase 0 v2 đạt Exit Criteria.

Nguyên tắc:
- ❌ Không thêm feature mới
- ❌ Không refactor
- ✅ Hotfix khẩn cấp được phép (commit message bắt đầu `hotfix(web-legacy):`)
- ✅ Đọc làm reference khi viết page mới ở `apps/web`

Xem [`apps/web-legacy/README.md`](./README.md) phía dưới cho doc gốc.

---
