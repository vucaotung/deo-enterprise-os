# Copy to .env (gitignored). Each agent file is provisioned manually
# by the operator with the api_token from `deo.agents` table.
AGENT_SLUG=finance-agent
AGENT_TOKEN=PASTE_UUID_FROM_deo.agents.api_token
API_BASE=https://api.enterpriseos.bond
